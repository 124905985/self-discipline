layui.use(['layer', 'form'], function () {
    var layer = layui.layer,
        form = layui.form,
        $ = layui.jquery;

    /**
     * 登录提交
     */
    form.on('submit(login)', function (data) {


        return false;
    });
});